
function sum(a, b) {
  // 매개변수 a와 b는 값이 들어갈 공간이다
  // 함수는 상자라서 열방법이 없다
  return a + b;
  // 반환 = 뱉어낸다
  // 리턴이 없으면 표시되지 않는다
}

console.log(sum(9,1));
console.log(sum(5,12));
console.log(sum(9,5950));
console.log(sum(84,5));
console.log(sum(942,95));
console.log(sum(4,6));
